const messages = {
    EXCEEDS_MAX_ALLOWED: '\nERROR: Period worked exceeds maximum allowed.',
    WRONG_NUMBER_ENTRIES: '\nERROR: Must have an odd number of entries greater than one.',
}
export default messages;